export default function UserDashboard() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-gray-800 mb-6">Dashboard Pengunjung</h1>

      {/* STATS CARDS */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <StatCard icon="🏃" label="Event Aktif" value="6" color="bg-blue-100" />
        <StatCard icon="🎫" label="Event Favorit" value="16" color="bg-blue-100" />
        <StatCard icon="📅" label="Event Diikuti" value="36" color="bg-blue-100" />
      </div>

      {/* ACARA TERDEKAT */}
      <div className="bg-white p-6 rounded-xl shadow-sm border border-gray-100">
        <h3 className="text-lg font-bold mb-4">Acara Terdekat</h3>
        <div className="space-y-4">
          <EventItem title="Seminar PPKS" date="11 April 2026" location="Auditorium Utama Polines" />
          <EventItem title="Workshop UI/UX" date="15 April 2026" location="Lab Komputer Polines" />
        </div>
      </div>
    </div>
  );
}

// Sub-komponen kecil agar kode rapi
function StatCard({ icon, label, value, color }: any) {
  return (
    <div className={`${color} p-6 rounded-2xl flex items-center justify-between`}>
      <span className="text-3xl">{icon}</span>
      <div className="text-right">
        <p className="text-4xl font-bold">{value}</p>
        <p className="text-gray-600 font-medium">{label}</p>
      </div>
    </div>
  );
}

function EventItem({ title, date, location }: any) {
  return (
    <div className="flex items-center gap-4 p-4 border rounded-xl hover:bg-gray-50 transition">
      <div className="w-32 h-20 bg-gray-200 rounded-lg overflow-hidden">
        {/* Placeholder Image */}
        <div className="w-full h-full bg-slate-400"></div>
      </div>
      <div className="flex-1">
        <h4 className="font-bold text-lg">{title}</h4>
        <p className="text-sm text-gray-500">{date} • {location}</p>
        <p className="text-xs text-blue-600 mt-1 font-medium">bem_polines</p>
      </div>
      <div className="bg-slate-900 text-white text-xs p-2 rounded-lg text-center">
        DIMULAI DALAM <br/> <span className="font-bold">1hr : 30m : 40s</span>
      </div>
    </div>
  );
}